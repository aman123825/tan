org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# AGP 9 built-in Kotlin provides the Kotlin DSL extension used by transitive
# Android plugins such as package:jni. Do not opt out here; the legacy
# kotlin-android plugin is intentionally absent from settings.gradle.kts.
